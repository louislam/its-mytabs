services:
    app:
        build: .
        image: mytabs-dev:local
        ports:
            # Host Port:Container Port  (dev instance, separate from prod on 7777)
            - "7778:47777"
        volumes:
            # Host Path:Container Path
            - ./data:/app/data
        restart: unless-stopped
